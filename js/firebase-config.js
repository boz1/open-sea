// ---------------------------------------------------------------------------
// Fill this in with YOUR Firebase project's config to enable live sync
// between everyone on the trip. Until you do, the app still works, but only
// saves data to your own browser (localStorage) — friends won't see edits.
//
// How to get these values (~5 minutes, free):
//   1. Go to https://console.firebase.google.com and create a project.
//   2. In the project, click "Build > Firestore Database" > Create database
//      > start in TEST MODE (fine for a small trip, see README for details).
//   3. Click the gear icon > Project settings > scroll to "Your apps" >
//      click the "</>" (web) icon > register an app (any nickname).
//   4. Firebase shows you a firebaseConfig object — paste its values below.
// ---------------------------------------------------------------------------

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
